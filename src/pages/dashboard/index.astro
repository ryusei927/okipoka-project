---
import AppLayout from "../../layouts/AppLayout.astro";
import RequireRole from "../../components/RequireRole.tsx";
import StoreDashboard from "../../components/StoreDashboard.tsx";
---
<AppLayout>
  <div class="container">
    <h1 class="section__title">店舗ダッシュボード</h1>
    <RequireRole client:load allow={["store","admin"]} fallback={<div class="muted">ログインしてください。</div>}>
      <StoreDashboard client:load />
    </RequireRole>
  </div>
</AppLayout>